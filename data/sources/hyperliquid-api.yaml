slug: hyperliquid-api
name: Hyperliquid info API
url: https://api.hyperliquid.xyz/info
platforms: [hyperliquid-outcomes]
kind: live-api
format: JSON POST
granularity: L2 book (20 levels/side), mids, outcome metadata
coverage:
  range: 2026–present
  completeness: partial
  known_gaps: "Outcome endpoints are served by the official API only — not by self-hosted nodes; no historical volume series."
access: free
description: "Hyperliquid's public info endpoint; serves outcome-market books and metadata alongside perps data."
